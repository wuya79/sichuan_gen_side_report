# 开发日志

## 2026-06-29

### 今日工作

#### 1. 发电侧日报系统搭建（v1.0）

- **背景**: 从零搭建四川燃煤电厂发电侧交易日报系统，与售电侧日报完全解耦
- **改动**:
  - 创建独立项目目录 `/home/ubuntu/.hermes/scripts/gen_side/`
  - `gen_txt.py`（600行）：从售电侧txt提取10个板块数据，调API补齐24h电价，输出发电侧专用txt
  - `generate_pdf.py`（700行）：Kimi API生成HTML + 4张matplotlib图表 + weasyprint转PDF
  - 后处理器 `inject_supplement_tables()`：注入7张缺失表格（天气前瞻×3、昨日偏差、来水偏差、系统备用、火电开机趋势）
  - 分析框替换器 `_fix_market_analysis()`：消除重复时段分解，替换为"市场综合研判"
- **产出**:
  - PDF: http://118.24.77.156:18080/reports/gen_side/gen_side_latest.pdf（25张表，4张图，561KB，12页）
  - TXT: http://118.24.77.156:18080/reports/gen_side_latest.txt（7371字符，10板块）
- **状态**: ✅ 完成

#### 2. 全维度审计与修复

- **审计范围**: 正则匹配、emoji影响、硬编码、取值过滤、板块完整性、代码bug、设计缺陷/鲁棒性共计7个维度
- **发现并修复的问题**:
  | # | 问题 | 严重度 | 修复 |
  |---|---|---|---|
  | 1 | `clear_dev`正则不匹配"日前36373MW vs 日内37058MW" | 🔴 | 加"日前"/"日内"后缀 |
  | 2 | `basin_line`/`trend_line`缺`re.MULTILINE`→永远为空 | 🟡 | ext()加flags参数 |
  | 3 | Chart 4竞争空间数据硬编码 | 🔴 | 从API获取+fallback |
  | 4 | Chart 3趋势标签硬编码`['06-23',...]` | 🔴 | 从txt解析日期范围动态生成 |
  | 5 | 竞争空间静态结论4行固定文本 | 🟡 | 从comp_data动态生成+fallback |
  | 6 | 火电开机趋势前缀"5.3"残留 | 🟡 | 加固正则去重 |
  | 7 | 售电侧txt不存在无友好报错 | 🟢 | SELL_TXT.exists()检查 |
  | 8 | inject_supplement_tables空值无检查 | 🟢 | if not raw or not html: return |
- **状态**: ✅ 全部修复完成

#### 3. Git仓库创建

- **位置**: `~/sichuan_gen_side_report/`
- **文件**: gen_txt.py (600行) + generate_pdf.py (700行) + README.md + .gitignore
- **提交**: `feat: 四川燃煤电厂发电侧交易日报系统 v1.0`
- **状态**: ✅ 完成

#### 4. Cron定时任务配置

- **时间**: 每日09:50自动运行
- **依赖链**: 09:30售电侧txt → 09:42售电侧PDF → 09:50发电侧
- **超时**: 720秒（12分钟）
- **周六冲突**: 周报10:00发电侧09:50→10分钟间隔→不冲突
- **安全**: 两步依赖（gen_txt.py失败则停止）、PID并发锁、Kimi重试机制
- **状态**: ✅ 完成

### 待办事项

- [x] 验证明天09:50第一次cron自动执行
- [ ] Chart 2出力堆叠图的solar/wind数组也可考虑从API动态获取（当前为经验估算）

### 备注

- 项目代码在 `~/sichuan_gen_side_report/`（独立仓库），运行文件在 `~/.hermes/scripts/gen_side/`
- 售电侧日报不受任何影响（零依赖、零写入）
- PDF生成瓶颈在Kimi API（~88秒），weasyprint约2秒，matplotlib约1秒

---

## 2026-07-02

### 今日工作

#### 1. 发电侧日报五维度审计与修复

- **审计范围**: API日期传错、提示词硬编码、设计缺陷/逻辑错误、数据耦合/臆想、其他问题共计5个维度21个问题
- **最严重问题（已全部修复）**:
  | # | 问题 | 严重度 | 修复方式 |
  |:-:|------|:------:|:---------|
  | 1-5 | 5处API日期传today而非yesterday | 🔴 | 改为yesterday_str |
  | 6-8 | 3处提示词硬编码臆想值"最高190元@12时"等 | 🔴 | 删除死值，改为data动态引用 |
  | 9 | 均价混源（txt均价vs API最高最低） | 🔴 | calc_avg优先用API计算 |
  | 10 | 最高最低小时索引错位（filter后index偏移） | 🔴 | 改为enumerate+next遍历原始列表 |
  | 12-13 | 竞争格局参考数据写死 | 🟡 | comp_data每日API拉取，兜底几乎不触发 |
  | 14 | price_range默认值"77-130" | 🟡 | 已删除默认值 |
  | 20 | generate_pdf正则匹配不到逐小时行 | 🟡 | gen_txt已输出逐小时电价表 |
  | 21 | spot/monthly始终死值12/160 | 🟡 | 改为从txt正则提取 |
  | 11 | 火电开机趋势兜底写死7个3700 | 🟢 | 改为动态用thermal_cap+thermal_units生成 |

- **最终结论**: 所有问题已全部修复，无需再做修改

#### 2. PDF输出URL改为带日期文件名

- **背景**: 用户要求所有日报链接使用带日期的文件名而非latest软链接
- **改动**:
  - `generate_pdf.py` L685-686: pdf_url/txt_url从`latest`改为`date_short`格式（YYYYMMDD）
  - `pdf_report_generator.py` L513: pdf_url从`latest`改为`dated_name`格式（YYYYMMDD）
- **Cron更新**:
  - 售电PDF生成（09:42）: prompt改写为使用source加载.env
  - 售电推送企微（09:45）: 改为从txt提取日期构造URL，不再写死latest
  - 发电PDF生成（09:50）: 直接用脚本输出的日期URL
- **验证**: 三个cron全部实际运行测试通过 ✅
- **Git提交**:
  - `sichuan_gen_side_report`: `0629fdd5` `fix: 发电侧日报API日期修正+输出URL改为带日期文件名`
  - `sichuan_daily_report_pdf`: `a61defa1` `fix: 售电侧日报PDF输出URL改为带日期文件名`
- **状态**: ✅ 完成

### 待办事项

- [ ] Chart 2出力堆叠图的solar/wind数组可考虑从API动态获取（当前为经验估算）
- [x] ~~验证明天09:50第一次cron自动执行~~（已验证通过）

### 备注

- PDF链接格式统一: `daily_YYYYMMDD.pdf` / `gen_side_YYYYMMDD.pdf`
- latest软链接保留向后兼容，但cron推送均使用带日期文件名
- 所有改动已commit并通过REST API推送到GitHub

## 2026-07-22

### Kimi图表幻觉修复

**问题**：每日发电侧PDF中4张图表只有2张进入报告，Kimi编造不存在的 `weather_forecast.png`，遗漏 `trend_7d.png` 和 `competition.png`。

**根因**：
1. prompt中 `xxx.png` 占位符被Kimi当模板自由填充
2. 只有2/4图表有section映射指令，其余2张无约束
3. 事后无任何图表引用校验

**修复**（generate_pdf.py, +117/-32行）：
- prompt修复：`xxx.png`→动态 `chart_instructions`（只列实际生成的图+section映射），4图全含
- 新增 `inject_chart_refs()`：事后删除幻觉引用+注入遗漏图表
- `find_section_by_keywords` / `insert_before_analysis` 提取为模块级函数
- 新增图表引用校验日志

**验证**：干跑+完整PDF生成通过，4图全在正确section，0编造0遗漏。

## 2026-09-01 Kimi 模型下线 + 完整性校验（大修）

**事故**：moonshot-v1-128k 被 Kimi 官方 8-31 下线（platform.kimi.com 模型列表页官方确认），PRIMARY 404 ×4 + Code Plan 300s 确定性 504 ×4，当天 PDF 全天缺失，用户点链接发现 404。

**修复**（generate_pdf.py 3 处 + 校验机制）：
- L34/L37 模型：OPEN→`kimi-k2.7-code-highspeed`、CODE→`kimi-for-coding-highspeed`（两模型为同一底层，双计费渠道）
- L74 PRIMARY 温度 0.2→1（新模型强制 temp=1，实测传 0.2 返回 400 "only 1 is allowed"）
- 新增 `validate_data_integrity`：hard 集（均价/净缺口/水电占比/火电出力，数字+单位组合匹配）+ soft 集（txt 全数字覆盖率 ≥85%）+ 防静默失效（txt 格式变化计入缺失）
- 生成改为最多 3 次尝试，重跑 prompt 告知缺失项；3 次不过 → 末次输出 + integrity_warning 告警
- 实跑验证 ×3：1分43秒/1分42秒/2分09秒，均一次通过（正常路径零额外费用）
- cron 09:50→10:00（避开 09:42 四川日报 PDF 的 Kimi 并发窗口）

**temp=1 漂移实证**：同输入两次生成 26表651KB vs 33表431KB（字体嵌入 610 vs 11 glyphs 为主因）。校验机制即为此而生。

**备选模型评估（均排除）**：k2.6 非思考模式（thinking disabled + temp 0.6）——8-9 分钟龟速（实测 28-78 tok/s）+ 两次实测第 2 次丢火电负载率；DeepSeek（temp 0.2 自由、无思考费用，但输出上限 ~19K tokens，扛不住 4 万 token 的 HTML）。

**skill 同步**：sichuan-gen-side-daily-report 陷阱 26（下线+温度约束）+ 陷阱 27（校验机制）已更新。

## 2026-09-02 上游断档连锁事故 + P0/P1 全链修复（大修）

**事故链**：上游 09-01 价格/水电/出清数据断档（type1/5/22 空，type14/16 午间恢复）→ gen_txt 兜底填0/模拟假数据 → 发电侧txt带病 → 校验失效 → PDF 误导交易员。用户发现：封面编造日期、板块八逐时表全0、昨日价格全0、月度108/滚动83取值错。

**根因层（gen_txt.py）**：
- 封面硬编码"日前市场X|实时市场昨日"行 → 删除
- 板块八 API 不可用时"模拟负荷曲线"公式生成24行假数据 → 删除，改"⚠️未取得"
- 13+2 字段 ext 兜底"0"→"" + `_disp()` 显示函数（0 只能来自真实数据）
- 火电开机趋势硬编码"3700" → 删除
- 趋势窗口错位：六板块=昨天终点(08-26~09-01)、5.3火电趋势=今天终点(08-27~09-02)，两组分离

**校验层（generate_pdf.py）**：
- validate v2 重写：字段级数值一致性（txt值 vs HTML值，剥标签+目录剔除+页码跳过+前缀排除防"占负荷"撞车）；"未取得"合法化；符号/千分位归一；负号保留
- 软覆盖率删除（与"未取得"占位冲突）
- 注入契约：find_section 改 h2标题优先匹配（Kimi 输出 sec1~sec10 不再失效），6张注入表恢复
- _gen_deviation_table 锚定"⑥→⑦"改"⑥→下一板块"（⑦板块消失时仍工作）
- parse_report_data 5字段锚定重写：avg_price 锚"昨日均价"（旧裸"均价"抓月内"9/3均价61"）；取不到→None
- Chart 4 硬编码假图（[88,91...]/34666/41343）→ API缺失时不生成
- Chart 3 净缺口7根同值柱 → 单值水平线+"非7日趋势"标注
- 告警外发：na_warning（上游缺失字段清单）+ integrity_detail 置顶输出；cron prompt 告警转发规则更新

**售电侧（daily_report.py，P1-D）**：6处缺失显式占位（首屏价格/火电负载率/入库↔水电/系统备用/来水偏差/⑦板块）+ 真0价 diff bug 修复

**校验实绩**（今日7次全链路实跑）：拦截 Kimi 误抄 月度108→滚动加权、填0→昨日均价、1.3%→负荷 三次真实编造。

**遗留**：DeepSeek 备用脚本 gen_side_ds_pdf.py 未同步（含旧0兜底+硬编码34666/1736），用户决定暂不管。
