# 开发日志

## 2026-07-04

### 今日工作

#### 1. 发电侧日报PDF — Chart 2数据源改造（type=2真实逐时数据）

- **背景**: Chart 2（各电源出力堆叠图）之前水电全天34666MW硬编码、光伏风电用固定模板，不反映当日实际数据，会误导读者。
- **改动**: 
  - 改为优先从type=2 API取昨日水电/火电/光伏/风电的96点实际数据，聚合为24小时画stackplot
  - 三级兜底机制：type=2 API → txt板块八提取水电逐时数据 → 硬编码fallback
  - 只画有数据的图层（API没返回光伏/风电就不画，避免臆想）
  - 修复了type=2返回值为str类型导致的 `>=` 类型比较错误
- **文件**: `generate_pdf.py`, `gen_side_ds_pdf.py`
- **状态**: ✅ 完成

#### 2. 发电侧日报PDF — Chart 3重写为三轴叠加趋势图

- **背景**: Chart 3（7日趋势）之前三个子图，净缺口只有今天一个值画7天水平线，毫无意义。
- **改动**:
  - 仿售电侧日报的三轴叠加图：电价（左轴蓝线）+ 水电占比（右轴绿线）+ 负荷（右轴偏移橙线，单位GW）
  - 去掉净缺口子图（无历史数据）
  - 数据从txt趋势行解析（电价/水电占比/负荷均有7天真实数据）
  - `parse_report_data`新增负荷7日趋势解析（trend_loads）
- **文件**: `generate_pdf.py`, `gen_side_ds_pdf.py`
- **状态**: ✅ 完成

#### 3. 发电侧日报PDF — 后处理修复price_24h重复引用

- **背景**: Kimi生成的HTML中把price_24h.png（24h电价走势图）重复放到板块三和板块四，PDF里出现两次同一张图。
- **改动**:
  - Prompt层：4张图的引用位置全部显式列出（板块四price_24h、板块五output_stack、板块七trend_7d、板块九competition），并强调"必须全部引用，不能重复遗漏"
  - 后处理层：新增`_remove_orphan_price_chart()`函数，按section id关键词匹配板块四，删除其之前的所有price_24h引用
  - 后处理连`<figure>`一起删除（不残留空白块）
  - 修复了<figure>扩展窗口30字符不够大的问题（改成80），以及at_figure判断分支
- **文件**: `generate_pdf.py`, `gen_side_ds_pdf.py`
- **状态**: ✅ 完成（已通过6个单元测试+真实HTML验证）

### 待办事项

- [ ] 明天cron跑完后验证新版Chart 2/Chart 3在PDF中的效果
- [ ] 观察Kimi是否会按要求引用全部4张图（prompt改进后的效果）

### 备注

Chart 3的净缺口历史数据缺失——`daily_trends.json`中没有归档net_gap。未来如果有需要，可以在售电侧日报的数据归档逻辑中加net_gap字段，慢慢积累后可以恢复净缺口子图。
